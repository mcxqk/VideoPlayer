name: VideoPlayer
main: com.github.squi2rel.vp.VideoPlayerPaperPlugin
version: ${version}
api-version: '${apiVersion}'
folia-supported: true
softdepend: [Residence]
authors: [squi2rel, cloudfl4re]
description: VideoPlayer server plugin bridge for the Fabric client mod.
commands:
  vlc:
    description: Manage VideoPlayer server notifications.
    usage: /videoplayer:vlc joinmessage
    permission: videoplayer.joinmessage
  vlcversion:
    description: Show connected VideoPlayer client versions.
    usage: /vlcversion
    permission: videoplayer.version
permissions:
  videoplayer.admin:
    description: Allows VideoPlayer plugin administration and custom metadata changes.
    default: op
    children:
      videoplayer.version: true
      videoplayer.joinmessage: true
  videoplayer.version:
    description: Allows viewing connected VideoPlayer client versions.
    default: op
  videoplayer.joinmessage:
    description: Allows toggling VideoPlayer join-version notifications.
    default: op
  videoplayer.action.play:
    description: Allows queueing videos.
    default: true
  videoplayer.action.seek:
    description: Allows dragging playback progress to seek videos.
    default: op
  videoplayer.action.sync:
    description: Allows syncing video progress.
    default: true
  videoplayer.action.vote_skip:
    description: Allows voting to skip.
    default: true
  videoplayer.action.force_skip:
    description: Allows force skipping.
    default: true
  videoplayer.action.set_skip_percent:
    description: Allows changing skip vote percent.
    default: true
  videoplayer.action.create_area:
    description: Allows creating video areas. Inside Residence this still requires owner/padd flags.
    default: true
  videoplayer.action.remove_area:
    description: Allows removing video areas. Inside Residence this still requires owner/padd flags.
    default: op
  videoplayer.action.create_screen:
    description: Allows creating video screens. Inside Residence this still requires owner/padd flags.
    default: true
  videoplayer.action.remove_screen:
    description: Allows removing video screens. Inside Residence this still requires owner/padd flags.
    default: op
  videoplayer.action.update_screen:
    description: Allows updating video screens.
    default: true
  videoplayer.action.set_uv:
    description: Allows changing screen UVs.
    default: true
  videoplayer.action.set_scale:
    description: Allows changing screen scale.
    default: true
  videoplayer.action.set_metadata:
    description: Allows changing screen metadata.
    default: true
  videoplayer.action.set_idle_play:
    description: Allows changing IdlePlay.
    default: true
  videoplayer.action.auto_sync:
    description: Allows auto sync requests.
    default: true
  videoplayer.action.open_menu:
    description: Allows opening the management UI.
    default: true
